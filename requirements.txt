fastapi==0.115.0
uvicorn[standard]==0.32.0
httpx==0.27.2
beautifulsoup4==4.12.3
lxml>=5.3.0
pydantic==2.9.2
pydantic-settings==2.0.3
python-dotenv==1.0.1
setuptools>=65.0.0

selenium==4.27.1
webdriver-manager==4.0.2
undetected-chromedriver==3.5.5
selenium-stealth==1.0.6
psutil==6.1.0

# Playwright (alternative to Selenium - more stable, no ChromeDriver issues)
playwright>=1.40.0
playwright-stealth>=2.0.0  # Anti-detection for Playwright

# Patchright — Playwright fork that closes the CDP Runtime.Enable leak and
# several smaller bot-detection signals. Drop-in API. Activated only when
# USE_PATCHRIGHT=true is set (see app/core/config.py); otherwise the worker
# continues to import stock playwright.
patchright>=1.49.0

# Optional: network interception
selenium-wire>=5,<6

blinker<1.8

# Queue / job state
celery[redis]>=5.4
redis>=5.0
tldextract>=5.0
structlog>=24.0
